
# smba
已经登录了部署了smba的服务器，使用普通用户登录的，不是管理员。确认自己登录的账号的smba是否正常的命令 （验证了，好像不对，win可以磁盘映射，但是这条命令也不正常）
``` bash
$ smbclient -L localhost -U chenjindou
Password for [MYGROUP\chenjindou]:

        Sharename       Type      Comment
        ---------       ----      -------
        print$          Disk      Printer Drivers
        IPC$            IPC       IPC Service (compilation-server server (Samba, Ubuntu))
        chenjindou      Disk      Home Directories
SMB1 disabled -- no workgroup available
```


